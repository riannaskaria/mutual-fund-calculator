# Frontend environment variables
# Copy to .env.local for local overrides. Never commit .env.local.

# Backend base URL — leave empty when using Vite's built-in proxy (development).
# For a separate frontend deployment (Vercel/Netlify), set this to your backend URL:
# VITE_API_BASE=https://your-app.onrender.com
VITE_API_BASE=
